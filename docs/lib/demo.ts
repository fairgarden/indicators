/**
 * The value that lets a visitor into the beta tree. In an app this would be
 * handed out by the server — set by a sign-in, say — and never appear in a
 * bundle; the demo has to write it from the browser, so here it is.
 */
export const BETA_COOKIE_VALUE = 'let-me-in'
